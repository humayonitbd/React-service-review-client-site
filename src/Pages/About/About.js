import React from 'react';
import useTitle from '../../hooks/useTitle';

const About = () => {
    // useTitle('about');
    return (
        <div>
            <h4>aboute page</h4>
        </div>
    );
};

export default About;