import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddService = () => {
    // useTitle('addService')
    return (
        <div>
            <h3>add service</h3>
        </div>
    );
};

export default AddService;